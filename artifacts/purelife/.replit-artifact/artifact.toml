kind = "web"
previewPath = "/"
title = "PureLife Wellness Club"
version = "1.0.0"
id = "artifacts/purelife"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21250

[services.development]
run = "pnpm --filter @workspace/purelife run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/purelife", "run", "build" ]
publicDir = "artifacts/purelife/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21250"
BASE_PATH = "/"
